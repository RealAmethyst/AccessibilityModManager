<Window x:Class="AccessibilityModManager.App.Views.UpdateAvailableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AccessibilityModManager.App.ViewModels"
        xmlns:controls="clr-namespace:AccessibilityModManager.App.Controls"
        d:DataContext="{d:DesignInstance vm:UpdateAvailableDialogViewModel}"
        mc:Ignorable="d"
        Title="Update available"
        Height="560" Width="640"
        MinHeight="360" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        AutomationProperties.Name="Update available">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Headline}"
                   FontSize="20" FontWeight="Bold"
                   AutomationProperties.HeadingLevel="Level1"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding Subheadline}"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2"
                   Text="What's new in this version:"
                   FontWeight="SemiBold"
                   AutomationProperties.HeadingLevel="Level2"
                   Margin="0,0,0,4"/>

        <!-- Editable TextBox + edit-blocking — the same pattern the mod ChangelogDialog uses
             so NVDA stays in focus mode and arrow keys move the caret line by line. -->
        <TextBox Grid.Row="3"
                 x:Name="ChangelogBox"
                 Text="{Binding ChangelogText, Mode=OneWay}"
                 controls:ReadOnlyText.IsEnabled="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="13"
                 AutomationProperties.Name="Release notes"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="InstallButton"
                    Content="Install update"
                    Click="Install_Click"
                    AutomationProperties.Name="Install update now"
                    Margin="0,0,8,0"
                    MinWidth="140"/>
            <Button Content="View on GitHub"
                    Click="ViewOnGitHub_Click"
                    AutomationProperties.Name="Open release page in browser"
                    Margin="0,0,8,0"
                    MinWidth="140"/>
            <Button x:Name="SkipButton"
                    Content="Skip for now"
                    Click="Skip_Click"
                    IsCancel="True"
                    IsDefault="True"
                    AutomationProperties.Name="Skip this update for now"
                    MinWidth="120"/>
        </StackPanel>
    </Grid>
</Window>
