<UserControl x:Class="AccessibilityModManager.App.Views.GamesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AccessibilityModManager.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:GamesListViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             AutomationProperties.Name="Games">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Games"
                   FontSize="24" FontWeight="Bold"
                   AutomationProperties.HeadingLevel="Level1"
                   Margin="0,0,0,8"/>

        <Button Grid.Row="1"
                Content="Refresh Games"
                Command="{Binding RefreshGamesCommand}"
                AutomationProperties.Name="Refresh games"
                HorizontalAlignment="Left"
                Margin="0,0,0,12"/>

        <ListView Grid.Row="2"
                  ItemsSource="{Binding Games}"
                  AutomationProperties.Name="Games"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:GameItemViewModel}">
                    <Border Padding="12" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding DisplayName}"
                                       FontSize="16" FontWeight="SemiBold"/>

                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding StatusText}"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{Binding StatusText}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                       Margin="0,4,0,0"
                                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}">
                                <Run Text="Plugins: "/>
                                <Run Text="{Binding PluginIds, Mode=OneWay, Converter={StaticResource ListToStringConverter}}"/>
                            </TextBlock>

                            <!-- Action buttons -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                        Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="View Details"
                                        Command="{Binding DataContext.OpenGameDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        IsEnabled="{Binding IsDetected}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='{}{0} details'}"
                                        Margin="0,0,8,0"/>
                                <Button Content="Open Folder"
                                        Command="{Binding DataContext.OpenGameFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        IsEnabled="{Binding IsDetected}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='{}{0} folder'}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   AutomationProperties.LiveSetting="Polite"
                   Margin="0,8,0,0"/>
    </Grid>
</UserControl>
