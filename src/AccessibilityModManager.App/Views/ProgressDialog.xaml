<Window x:Class="AccessibilityModManager.App.Views.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AccessibilityModManager.App.ViewModels"
        xmlns:controls="clr-namespace:AccessibilityModManager.App.Controls"
        d:DataContext="{d:DesignInstance vm:ProgressDialogViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="320" Width="560"
        MinHeight="200" MinWidth="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        AutomationProperties.Name="Progress">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Deliberately NOT a live region. StatusText carries the byte counter
             ("Downloading... 1,234 / 5,678 KB") and is rewritten after every 80 KB read, so
             announcing it would talk over the entire download. It stays on screen; the phase
             line below is the one that speaks. -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   TextWrapping="Wrap"
                   FontSize="14"
                   Margin="0,0,0,12"/>

        <ProgressBar Grid.Row="1"
                     Value="{Binding Percentage}"
                     Minimum="0" Maximum="100"
                     Height="24"
                     AutomationProperties.Name="Progress"
                     Margin="0,0,0,8"/>

        <!-- THE announcement stream for this dialog. StepDescription is a stable phase label
             ("Downloading package", "Running pre-install script: setup.ps1") that changes only when
             the install genuinely moves on, so it speaks once per phase instead of once per chunk.
             Everything else here is readable but silent. -->
        <TextBlock Grid.Row="2"
                   controls:LiveRegion.Text="{Binding StepDescription}"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   AutomationProperties.LiveSetting="Polite"/>

        <!-- Not live: the view model mirrors this into StepDescription, which announces it. Marking
             both spoke the same sentence twice. -->
        <TextBlock Grid.Row="3"
                   Text="{Binding ScriptStatusHeader}"
                   FontWeight="SemiBold"
                   AutomationProperties.HeadingLevel="Level2"
                   Visibility="{Binding IsScriptRunning, Converter={StaticResource BoolToVisConverter}}"
                   Margin="0,12,0,4"/>

        <TextBox Grid.Row="4"
                 x:Name="OutputBox"
                 Text="{Binding ScriptOutput, Mode=OneWay}"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 TextChanged="OutputBox_TextChanged"
                 AutomationProperties.Name="Script output"
                 Visibility="{Binding IsScriptRunning, Converter={StaticResource BoolToVisConverter}}"/>

        <Button Grid.Row="5"
                Content="Cancel"
                Command="{Binding CancelCommand}"
                IsEnabled="{Binding IsCancellable}"
                AutomationProperties.Name="Cancel"
                HorizontalAlignment="Right"
                MinWidth="80"
                Margin="0,12,0,0"/>
    </Grid>
</Window>
